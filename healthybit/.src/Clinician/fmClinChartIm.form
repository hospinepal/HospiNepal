# Gambas Form File 3.0

{ Form Form
  MoveScaled(0,0,99,76)
  Text = ("Chart View")
  { mnufile Menu
    Text = ("File")
    { mnusave Menu
      Text = ("Save to PNG")
    }
    { mnuexport Menu
      Text = ("HTML View")
    }
  }
  { Panel1 Panel
    MoveScaled(0,0,98,72)
    { ScrollView1 ScrollView
      MoveScaled(0,0,97,71)
      Expand = True
      Arrangement = Arrange.Vertical
      { grwchart DrawingArea
        MoveScaled(1,1,95,69)
        Background = Color.White
        Expand = True
        Border = Border.Plain
        Cached = True
      }
    }
  }
}
