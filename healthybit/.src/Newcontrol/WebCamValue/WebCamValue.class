' Gambas class file

Static Public Sub _call(sDevice As String) As String

  If modBasic.$CamMethod = "video4linux" Then
    Return fmWebCamOld.Run(sDevice)
  Else
    Return fmWebCamValue.Run(sDevice)
  Endif

End
