' Gambas class file

Private $sTitle As String
Private $ItemList As String[]

Private $sCategoryList As String[]
Private $sValueList As String[]

Private $aMyFields As String[]
Private $sValue As String

Public Sub Run(Prompt As String, ItemList As String[]) As String

  $sTitle = Prompt
  $ItemList = ItemList

  If Me.ShowModal() Then Return $sValue

End

Public Sub Form_Open()

  modGeneralMain.ArrangeFormCentre(Me, "False")
  Me.Text = $sTitle
  GetItemListData()
  ComboBox1.List = $sCategoryList
  lstletter.List = modString.GetFirstLetterArray($sValueList)
  FillItemGrid($sValueList)

End

Private Sub GetItemListData()

  Dim xx As String
  Dim asx As String[]
  Dim xval As String[]

  $sCategoryList = New String[]
  xval = New String[]
  For Each xx In $ItemList
    If xx Then
      asx = Split(xx, "@")

      If asx[0] Then
        If $sCategoryList.Count = 0 Then
          $sCategoryList.Add(asx[0])
        Else
          If $sCategoryList.Exist(asx[0]) = False Then
            $sCategoryList.Add(asx[0])
          Endif
        Endif
      Endif

      If asx[1] Then
        If xval.Count = 0 Then
          xval.Add(asx[1])
        Else
          If xval.Exist(asx[1]) = False Then
            xval.Add(asx[1])
          Endif
        Endif
      Endif

    Endif
  Next
  $sValueList = xval

End

Private Sub FillItemGrid(sList As String[])

  GridView1.Clear()
  $aMyFields = modString.SelectStringArrayToText(txtname.Text, sList, False)
  GridView1.Columns.Count = 1
  GridView1.Rows.Count = $aMyFields.Count
  GridView1.Rows.Height = 25 * modBasic.$AppHeightRatio
  GridView1.Columns[0].Width = 600 * modBasic.$AppWidthRatio
  GridView1.Row = 0

End

Public Sub GridView1_Data(Row As Integer, Column As Integer)

  GridView1.Data.Text = $aMyFields[Row]

End

Public Sub lstletter_Click()

  txtname.Text = lstletter.Text
  FillItemGrid($sValueList)

End

Public Sub GridView1_KeyRelease()

  If GridView1.Rows.Count > 0 Then
    If Key.Code = Key.Return Or If Key.Code = Key.Enter Or If Key.Code = Key.AltKey Then
      $sValue = GridView1[GridView1.Row, 0].Text
      Me.Close(True)
    Endif
  Endif

End

Public Sub btnok_Click()

  If GridView1.Rows.Count > 0 Then
    $sValue = GridView1[GridView1.Row, 0].Text
    Me.Close(True)
  Endif

End

Public Sub Form_KeyRelease()

  If Key.Code = Key.Esc Then
    Me.Close
  Endif

End

Public Sub ComboBox1_Click()

  Dim xval As String[]
  Dim xx As String
  Dim asx As String[]

  If ComboBox1.Text Then
    txtname.Text = ""
    xval = New String[]
    For Each xx In $ItemList

      If xx Then
        asx = Split(xx, "@")
        If asx[0] = ComboBox1.Text Then
          If xval.Count = 0 Then
            xval.Add(asx[1])
          Else
            If xval.Exist(asx[1]) = False Then
              xval.Add(asx[1])
            Endif
          Endif
        Endif
      Endif

    Next
  Endif
  $sValueList = xval
  lstletter.List = modString.GetFirstLetterArray($sValueList)
  FillItemGrid($sValueList)

End
